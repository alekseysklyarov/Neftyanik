@page "/Administration/Members/Finance/{id:int}/CreateCharge"
@model Neftyanik.Portal.Web.Pages.Administration.Members.Finance.CreateChargeModel
@{
    ViewData["Title"] = $"Начисление: {Model.Member.FullName}";
}

<div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4">
    <div>
        <h1 class="h2 mb-1">Новое начисление</h1>
        <p class="text-muted mb-0">Член товарищества: @Model.Member.FullName</p>
    </div>
    <div class="d-flex gap-2 flex-wrap">
        <a class="btn btn-outline-primary" asp-page="/Administration/Members/Finance" asp-route-id="@Model.Member.Id">К финансам участника</a>
        <a class="btn btn-outline-secondary" asp-page="/Administration/Members/Details" asp-route-id="@Model.Member.Id">Карточка члена товарищества</a>
    </div>
</div>

<div class="card shadow-sm mb-4">
    <div class="card-body">
        <div class="row g-3">
            <div class="col-md-4">
                <p class="mb-1"><strong>Активные участки:</strong> @Model.Member.ActivePlotsCount</p>
                <p class="mb-0"><strong>Телефон:</strong> @(string.IsNullOrWhiteSpace(Model.Member.PhoneNumber) ? "—" : Model.Member.PhoneNumber)</p>
            </div>
            <div class="col-md-4">
                <p class="mb-0"><strong>Email:</strong> @(string.IsNullOrWhiteSpace(Model.Member.Email) ? "—" : Model.Member.Email)</p>
            </div>
            <div class="col-md-4">
                <p class="mb-0 text-muted">Начисление будет создано для выбранного участка из текущих владений участника.</p>
            </div>
        </div>
    </div>
</div>

<div class="card shadow-sm">
    <div class="card-body">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
            <div class="row g-3">
                <div class="col-md-6">
                    <label asp-for="Input.PlotId" class="form-label"></label>
                    <select asp-for="Input.PlotId" asp-items="Model.PlotOptions" class="form-select">
                        <option value="">Выберите участок</option>
                    </select>
                    <span asp-validation-for="Input.PlotId" class="text-danger"></span>
                </div>
                <div class="col-md-6">
                    <label asp-for="Input.ChargeTypeId" class="form-label"></label>
                    @if (Model.HasSingleChargeType)
                    {
                        <input type="hidden" asp-for="Input.ChargeTypeId" />
                        <input class="form-control" value="@Model.SingleChargeTypeText" readonly />
                    }
                    else
                    {
                        <select asp-for="Input.ChargeTypeId" asp-items="Model.ChargeTypeOptions" class="form-select">
                            <option value="">Выберите тип начисления</option>
                        </select>
                    }
                    <span asp-validation-for="Input.ChargeTypeId" class="text-danger"></span>
                </div>
                <div class="col-md-4">
                    <label asp-for="Input.Amount" class="form-label"></label>
                    <input asp-for="Input.Amount" class="form-control" type="number" step="0.01" min="0.01" />
                    <span asp-validation-for="Input.Amount" class="text-danger"></span>
                </div>
                <div class="col-md-4">
                    <label asp-for="Input.ChargeDate" class="form-label"></label>
                    <input asp-for="Input.ChargeDate" class="form-control" type="date" />
                    <span asp-validation-for="Input.ChargeDate" class="text-danger"></span>
                </div>
                <div class="col-md-4">
                    <label asp-for="Input.DueDate" class="form-label"></label>
                    <input asp-for="Input.DueDate" class="form-control" type="date" />
                    <span asp-validation-for="Input.DueDate" class="text-danger"></span>
                </div>
                <div class="col-12">
                    <label asp-for="Input.Description" class="form-label"></label>
                    <textarea asp-for="Input.Description" class="form-control" rows="4"></textarea>
                    <span asp-validation-for="Input.Description" class="text-danger"></span>
                </div>
            </div>
            <div class="d-flex gap-2 mt-4 flex-wrap">
                <button type="submit" class="btn btn-primary">Сохранить начисление</button>
                <a class="btn btn-outline-secondary" asp-page="/Administration/Members/Finance" asp-route-id="@Model.Member.Id">Отмена</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
